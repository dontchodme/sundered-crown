# RENAMED — this design is now `06-docs/v62/duskreave-design-v62.md`

The umbral scythe was named **Duskreave** and its ultimate **Scour** on
2026-09-01, and the document moved to the project's convention of titling a
design after its relic. **Nothing was lost — the file at the new path is this
document, complete, and it is the one to read.**

This placeholder exists because the device bridge in the session that made the
rename could not delete files. Delete it whenever convenient.
