# 07-shorts is NOT in this zip

The full v30 tree is 160 MB and the handoff channel caps at 30 MiB, so
`07-shorts/` — the v27 sample slate and this session's two clips — was left
out. Everything else is here and complete: the chain, both merged patches,
every builder, every probe, the docs, and `01-live` untouched.

Restore it by copying `07-shorts/` across from your `sc-seed-v27` tree. The two
new clips were delivered separately in the conversation:

```
07-shorts/lastlight/lastlight-v-gravemourn.mp4   Lastlight (blade 17.5) on the
                                                 lastlight-only build, 36.0s
07-shorts/lastlight/lastlight-v-axiom-v30.mp4    THE COMPOSED TIP — Lastlight v
                                                 the shattered Axiom under the
                                                 health HUD, seed 88489753,
                                                 51.1s, Lastlight on 20 hp
```

Nothing in the build depends on `07-shorts/` existing.
